<?xml version="1.0" encoding="utf-8"?>
<!-- Header pinned at the top of the autofill drop-down. Single brand
     mark (small shield+keyhole vector, NOT the launcher logo) + brand
     name + count. Keeps the "this is VaultSnap" signal in exactly one
     place so the suggestion list below isn't a mosaic of duplicate
     brand badges. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:background="@color/autofill_brand_bg"
    android:paddingStart="14dp"
    android:paddingEnd="14dp"
    android:paddingTop="8dp"
    android:paddingBottom="8dp">

    <ImageView
        android:layout_width="14dp"
        android:layout_height="14dp"
        android:layout_marginEnd="8dp"
        android:src="@drawable/ic_autofill_brand"
        android:tint="@color/autofill_brand"
        android:contentDescription="@null" />

    <TextView
        android:layout_width="0dp"
        android:layout_weight="1"
        android:layout_height="wrap_content"
        android:text="@string/autofill_brand_name"
        android:textSize="12sp"
        android:textStyle="bold"
        android:letterSpacing="0.04"
        android:textColor="@color/autofill_brand" />

    <TextView
        android:id="@+id/autofill_header_count"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="11sp"
        android:textColor="@color/autofill_text_dim"
        android:letterSpacing="0.04" />
</LinearLayout>

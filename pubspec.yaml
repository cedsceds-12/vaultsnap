name: vault_snap
description: "VaultSnap - a local-only password manager. No internet, no cloud, no accounts."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.0.0+1

environment:
  sdk: ^3.11.5

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  cryptography: ^2.9.0
  # 9.x uses standard MethodChannel; 10.x pulls in the `jni` Dart package
  # whose gradle plugin breaks on stock Android config. We don't need
  # JNI-level perf for wrapping a 32-byte VMK.
  flutter_secure_storage: ^9.2.4
  local_auth: ^2.3.0
  path: ^1.9.1
  path_provider: ^2.1.5
  pointycastle: ^3.9.1
  flutter_riverpod: ^3.3.1
  sqflite: ^2.4.2+1
  encrypt: ^5.0.3
  # Phase 9 — encrypted attachments. Platform document picker used to
  # select files for vault attachments. No new permissions added (the
  # picker uses the system Storage Access Framework on Android and the
  # native document picker on iOS — both grant per-file URI access only,
  # never broad storage scope).
  file_picker: ^8.0.0
  # Phase 9 — in-app PDF preview deferred to v1.1.
  # Tried `pdfx ^2.6.0` and `pdfrx ^2.2.24` — both fail Gradle's
  # `assembleDebug` with "Cannot query the value of this provider"
  # at `:pdfx:compileDebugJavaWithJavac` / `:pdfium_flutter:...`.
  # Same class of unfixed AGP 8.x compatibility bug in the native
  # backend. Tried a `namespace`-fallback workaround in
  # `android/build.gradle.kts` — the error fires earlier than that
  # hook can run. `flutter_pdfview` was rejected because it writes
  # the decrypted bytes to a temp file on Android (its `pdfData`
  # path uses `PdfRenderer` which requires a `ParcelFileDescriptor`
  # backed by an on-disk file — defeats the privacy guarantee).
  # For v1, PDFs go through the same "Export decrypted…" path as
  # any other non-image attachment: user explicitly approves a
  # plaintext write to a SAF location, which they can then open in
  # any external app. Re-add a PDF renderer in v1.1 once the plugin
  # ecosystem stabilises (or implement a custom platform channel
  # that streams decrypted bytes through a memory pipe to
  # `PdfRenderer`, never touching disk).

# path_provider_android 2.3.x switched to a JNI-based implementation whose
# gradle plugin (`jni:compileDebugJavaWithJavac`) cannot resolve required
# providers on stock Android Gradle config and crashes the build.
# Pin to the last MethodChannel release until the upstream fix lands.
dependency_overrides:
  path_provider_android: 2.2.16

dev_dependencies:
  flutter_test:
    sdk: flutter

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

  # Test-only — provides an in-memory sqflite implementation usable from
  # plain Dart tests (no Flutter binding, no real device). Used by the
  # backup-roundtrip tests in test/services/backup_*_test.dart so they
  # can stand up a real VaultDatabase instance instead of mocking sqflite.
  sqflite_common_ffi: ^2.3.5

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package
